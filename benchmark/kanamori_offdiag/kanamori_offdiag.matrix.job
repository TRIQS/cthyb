#!/bin/sh
#$ -S /bin/sh
#$ -N kanamori_offdiag.matrix
#$ -pe mpi 128
#$ -q amd

if [ -n "${SGE_TASK_ID}" ]
then
    echo "Running the MPI version ..."
    mpirun -machinefile $TMPDIR/machines -np ${NSLOTS} pytriqs kanamori_offdiag.matrix.py
else
    pytriqs kanamori_offdiag.matrix.py
fi
