#!/bin/sh
#$ -S /bin/sh
#$ -N kanamori.matrix
#$ -pe mpi 128
#$ -q amd

if [ -n "${SGE_TASK_ID}" ]
then
    echo "Running the MPI version ..."
    mpirun -machinefile $TMPDIR/machines -np ${NSLOTS} pytriqs kanamori.matrix.py
else
    pytriqs kanamori.matrix.py
fi
