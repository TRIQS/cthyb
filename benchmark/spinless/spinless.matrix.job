#!/bin/sh
#$ -S /bin/sh
#$ -N spinless.matrix 
#$ -pe mpi 128
#$ -q amd

if [ -n "${SGE_TASK_ID}" ]
then
	echo "Running the MPI version ..."
	mpirun -machinefile $TMPDIR/machines -np ${NSLOTS} pytriqs spinless.matrix.py
else
	pytriqs spinless.matrix.py
fi
