#!/bin/sh
#$ -S /bin/sh
#$ -N anderson.matrix 
#$ -pe mpi 128
#$ -q amd

if [ -n "${SGE_TASK_ID}" ]
then
	echo "Running the MPI version ..."
	mpirun -machinefile $TMPDIR/machines -np ${NSLOTS} pytriqs anderson.matrix.py
else
	pytriqs anderson.matrix.py
fi
